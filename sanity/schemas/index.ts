import { localeBlock, localeString, localeText, sectionIntro, seo } from "./objects";
import { documents } from "./documents";

export const schemaTypes = [localeString, localeText, localeBlock, sectionIntro, seo, ...documents];
