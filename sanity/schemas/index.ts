import { localeBlock, localeString, localeText, seo } from "./objects";
import { documents } from "./documents";

export const schemaTypes = [localeString, localeText, localeBlock, seo, ...documents];
